# Argument, part 7

Let H(x) = -x log x - (1-x) log(1-x). Stirling's formula gives

(1/n) log binom(N_n,K_n) -> A H(B/A)

whenever N_n/n -> A and K_n/n -> B with 0 < B < A.

In part 6, the three all-on-hull factors add

H(1/4) + H(1/2) + (3/2) H(1/3) = 2 log 2 + (3/4) log 3.

The binomial quotient adds

3 H(1/2) - 3 H(1/4) = -3 log 2 + (9/4) log 3.
