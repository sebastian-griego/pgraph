# Argument, part 3

Each graph with q+1 edges is counted exactly q+1 times in the pair count, one time for each removed edge. Multiplying the rank inequality from q=a to b-1 gives

g_b(P) >= g_a(P) binom(M,b) / binom(M,a).                (3)
