# Argument, part 5

The choices in part 4 give distinct graphs with s+d edges, so for m=s+d,

g_m(C_n) >= binom(n,s) * 1/(d+1) * binom(n-3,d) * binom(n+d-1,d).   (4)

Now set s_n = floor(n/4), d_n = floor(n/2), a_n = s_n+d_n, L_n = 3n-h-3, and b_n = floor(L_n/2). For all large n, d_n <= n-3 and a_n <= b_n <= L_n.
