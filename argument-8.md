# Argument, part 8

The factor 1/(d_n+1) is subexponential. Therefore part 6 and part 7 imply

liminf (1/n) log m_h(n) >= 3 log 3 - log 2 = log(27/2).

Exponentiating gives

liminf m_h(n)^(1/n) >= 27/2.

Finally, 27/2 - (6 + 4 sqrt(2)) = 15/2 - 4 sqrt(2) > 0. This proves the desired strict exponential improvement.
